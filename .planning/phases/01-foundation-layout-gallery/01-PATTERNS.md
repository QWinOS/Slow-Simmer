# Phase 1: Foundation, Layout & Gallery - Pattern Map

**Mapped:** 2026-07-02
**Files analyzed:** 15 (3 modified, 9 new custom, 3 shadcn-generated)
**Analogs found:** 3 with excerpts / 15 total

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `app/layout.tsx` | layout | request-response | `app/layout.tsx` (existing) | exact (modify) |
| `app/page.tsx` | component | request-response | `app/page.tsx` (existing) | role-match (rewrite) |
| `app/globals.css` | config | — | `app/globals.css` (existing) | exact (modify) |
| `.env.local` | config | — | none (new file) | no analog |
| `components/NavBar.tsx` | component | event-driven | none (no client components exist) | no analog |
| `components/HeroSection.tsx` | component | request-response | none (no server components exist) | no analog |
| `components/AboutSection.tsx` | component | request-response | none | no analog |
| `components/GalleryGrid.tsx` | component | CRUD (Drive API) | none | no analog |
| `components/GalleryLightbox.tsx` | component | event-driven | none | no analog |
| `components/VideoGrid.tsx` | component | event-driven | none | no analog |
| `components/VideoThumbnail.tsx` | component | request-response | none | no analog |
| `components/FormPlaceholder.tsx` | component | request-response | none | no analog |
| `components/Footer.tsx` | component | request-response | none | no analog |
| `components/ui/button.tsx` | component | request-response | shadcn-generated | no analog |
| `components/ui/dialog.tsx` | component | event-driven | shadcn-generated | no analog |

## Pattern Assignments

### `app/layout.tsx` (layout, request-response) — MODIFY EXISTING

**Analog:** Current `app/layout.tsx` (lines 1-38) — the existing root layout file.

**Imports pattern** (lines 1-4):
```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono, Public_Sans, Raleway } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";
```

**Font loading pattern** (lines 6-18):
```typescript
const ralewayHeading = Raleway({subsets:['latin'],variable:'--font-heading'});

const publicSans = Public_Sans({subsets:['latin'],variable:'--font-sans'});

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});
```

**Export metadata pattern** (lines 20-23):
```typescript
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
```

**RootLayout component pattern** (lines 25-37):
```typescript
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={cn("h-full", "antialiased", geistSans.variable, geistMono.variable, "font-sans", publicSans.variable, ralewayHeading.variable)}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
```

**Required changes for Phase 1:**
1. Replace font imports: `Geist, Geist_Mono, Public_Sans, Raleway` → `Playfair_Display_SC, Karla`
2. Replace font variable declarations with Playfair Display SC (`--font-heading`) and Karla (`--font-sans`)
3. Update `metadata.title` and `metadata.description` for supper club branding
4. Remove `geistSans`, `geistMono` variables from `<html>` className
5. Wrap children in `<ThemeProvider>` from `next-themes`
6. Add `suppressHydrationWarning` to `<html>` for next-themes

---

### `app/globals.css` (config, —) — MODIFY EXISTING

**Analog:** Current `app/globals.css` (lines 1-130) — the existing global stylesheet.

**Tailwind v4 import pattern** (lines 1-3):
```css
@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";
```

**Dark variant pattern** (line 5):
```css
@custom-variant dark (&:is(.dark *));
```

**Theme variable mapping pattern** (lines 7-49):
```css
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-sans);
  --font-mono: var(--font-geist-mono);
  --font-heading: var(--font-heading);
  /* ... all shadcn color tokens mapped to CSS variables ... */
  --radius-sm: calc(var(--radius) * 0.6);
  --radius-md: calc(var(--radius) * 0.8);
  --radius-lg: var(--radius);
  /* ... radius scale ... */
}
```

**Light mode variable pattern** (lines 51-84):
```css
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.141 0.005 285.823);
  --primary: oklch(0.527 0.154 150.069);
  /* ... all shadcn CSS variables in OKLCH ... */
  --radius: 0.875rem;
}
```

**Dark mode variable pattern** (lines 86-118):
```css
.dark {
  --background: oklch(0.141 0.005 285.823);
  --foreground: oklch(0.985 0 0);
  --primary: oklch(0.448 0.119 151.328);
  /* ... dark mode overrides ... */
}
```

**Base layer pattern** (lines 120-130):
```css
@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
  html {
    @apply font-sans;
  }
}
```

**Required changes for Phase 1:**
1. Replace OKLCH values in `:root` with hex values from UI-SPEC (warm red/gold palette)
2. Replace OKLCH values in `.dark` with dark mode hex values from UI-SPEC
3. Add `--brand-primary: #DC2626` as a custom property for decorative brand elements
4. Keep the `@theme inline {}` structure — just update the variable values
5. Add `scroll-behavior: smooth` to `html` for smooth scroll navigation
6. Add `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }`

---

### `app/page.tsx` (component, request-response) — REWRITE

**Analog:** Current `app/page.tsx` (lines 1-65) — serves as a structural template for the Server Component shell pattern.

**Analog pattern to adapt** (lines 3-6):
```typescript
export default function Home() {
  return (
    <div className="flex flex-col flex-1 items-center justify-center ...">
      <main className="flex flex-1 w-full max-w-3xl ...">
```

**New pattern to follow** (from RESEARCH.md Pattern 1 — Server Component Islands pattern):

The page becomes a Server Component that composes all sections as children:

```typescript
import NavBar from '@/components/NavBar'
import HeroSection from '@/components/HeroSection'
import AboutSection from '@/components/AboutSection'
import GallerySection from '@/components/GallerySection'
import VideoSection from '@/components/VideoSection'
import FormPlaceholder from '@/components/FormPlaceholder'
import Footer from '@/components/Footer'

export default function HomePage() {
  return (
    <>
      <NavBar />
      <main>
        <HeroSection />
        <AboutSection />
        <GallerySection />
        <VideoSection />
        <FormPlaceholder />
      </main>
      <Footer />
    </>
  )
}
```

---

### `components/NavBar.tsx` (component, event-driven) — NEW

**Analog:** No custom client component exists in the codebase. This is the first `'use client'` component in the project.

**Pattern source:** RESEARCH.md — Client Component Islands architecture.
**Pattern to follow:** 
- `'use client'` directive as first line
- Uses `useState`, `useEffect`, `useCallback` for scroll state and IntersectionObserver
- Imports from `@/lib/utils` for `cn()` class merging
- Uses shadcn `Button` component for CTA
- Uses remixicon icons for hamburger menu
- No direct codebase analog exists

---

### `components/HeroSection.tsx` (component, request-response) — NEW

**Analog:** No custom Server Component exists. The existing `app/page.tsx` (lines 1-65) demonstrates Server Component structure with Tailwind classes.

**Pattern to follow:** Pure Server Component — no `'use client'` directive. Renders static JSX with TailwindCSS utility classes. Uses Playfair Display SC via `font-heading` CSS variable.

---

### `components/AboutSection.tsx` (component, request-response) — NEW

**Analog:** Same as HeroSection — no existing custom Server Component.

**Pattern to follow:** Pure Server Component. Same pattern as HeroSection. Each section wrapper gets an `id` attribute for smooth-scroll anchor targeting.

---

### `components/GalleryGrid.tsx` (component, CRUD) — NEW

**Analog:** No client component with external API fetch exists in the codebase.

**Pattern source:** RESEARCH.md — Google Drive Public Image Fetch pattern (Pattern 2, lines 287-316).

**Core data fetching pattern** (from RESEARCH.md):
```typescript
const FOLDER_ID = process.env.NEXT_PUBLIC_DRIVE_FOLDER_ID!
const API_KEY = process.env.NEXT_PUBLIC_GOOGLE_API_KEY!

async function fetchGalleryImages(): Promise<DriveFile[]> {
  const res = await fetch(
    `https://www.googleapis.com/drive/v3/files?` +
    `q='${FOLDER_ID}'+in+parents+and+mimeType+contains+'image/'` +
    `&key=${API_KEY}&fields=files(id,name,mimeType,webContentLink)`
  )
  if (!res.ok) throw new Error('Drive API failed')
  const data = await res.json()
  return data.files || []
}
```

**States to implement:** loading (skeleton shimmer), empty ("No photos yet"), error ("Unable to load gallery"), loaded (image grid with lazy loading)

---

### `components/GalleryLightbox.tsx` (component, event-driven) — NEW

**Analog:** No dialog/overlay component exists in the codebase.

**Pattern source:** RESEARCH.md — shadcn `Dialog` component recommendation (lines 322-325). UI-SPEC provides interaction specifications.

**Core pattern:** Uses shadcn `Dialog` from `@/components/ui/dialog`. Manages current image index as state. Implements keyboard navigation (arrow keys, Escape) and touch swipe.

---

### `components/VideoGrid.tsx` (component, event-driven) — NEW

**Analog:** No client component with conditional rendering exists.

**Pattern source:** RESEARCH.md — YouTube/Instagram embed patterns (lines 403-425). UI-SPEC for interaction states.

**Core embed URL patterns** (from RESEARCH.md):
```typescript
// YouTube privacy-enhanced embed
export function getYouTubeEmbedUrl(videoId: string, autoplay = false): string {
  const params = new URLSearchParams({
    rel: '0',
    modestbranding: '1',
  })
  if (autoplay) params.set('autoplay', '1')
  return `https://www.youtube-nocookie.com/embed/${videoId}?${params.toString()}`
}

// Instagram embed
export function getInstagramEmbedUrl(postId: string): string {
  return `https://www.instagram.com/p/${postId}/embed`
}
```

---

### `components/VideoThumbnail.tsx` (component, request-response) — NEW

**Analog:** No card/thumbnail component exists.

**Pattern source:** UI-SPEC component inventory. Renders a thumbnail image with a play button overlay (remixicon `play-circle-fill`). Accepts `onPlay` callback prop to notify parent (VideoGrid) when user clicks play.

---

### `components/FormPlaceholder.tsx` (component, request-response) — NEW

**Analog:** No Server Component with static content exists beyond the current page.

**Pattern to follow:** Pure Server Component. Simple centered card with "Registration form coming soon" text.

---

### `components/Footer.tsx` (component, request-response) — NEW

**Analog:** No Server Component with static content exists beyond the current page.

**Pattern to follow:** Pure Server Component. Static content with copyright, social links.

---

### `components/ui/*.tsx` (component) — shadcn-generated

**Pattern source:** These files are generated by `npx shadcn@latest add button dialog card separator skeleton`. They follow the project's shadcn preset (radix-nova, remixicon icons, zinc base color). No manual analog needed.

---

### `.env.local` (config) — NEW

**Analog:** No `.env` files exist in the project. `.env*` is in `.gitignore` (line 34).

**Pattern to follow:** Standard Next.js `.env.local` format:
```
NEXT_PUBLIC_GOOGLE_API_KEY=your_api_key_here
NEXT_PUBLIC_DRIVE_FOLDER_ID=your_folder_id_here
```

All env vars use `NEXT_PUBLIC_` prefix because they're accessed from client-side code.

---

## Shared Patterns

### Font Loading via `next/font/google`
**Source:** `app/layout.tsx` (lines 1-18)
**Apply to:** `app/layout.tsx` (modify)
```typescript
import { Playfair_Display_SC, Karla } from "next/font/google";

const playfairDisplay = Playfair_Display_SC({
  subsets: ["latin"],
  weight: ["400", "700"],
  variable: "--font-heading",
});

const karla = Karla({
  subsets: ["latin"],
  weight: ["400", "700"],
  variable: "--font-sans",
});
```
Apply CSS variables via className on `<html>`: `className={cn(playfairDisplay.variable, karla.variable)}`

### CSS Theming with TailwindCSS v4
**Source:** `app/globals.css` (lines 1-130)
**Apply to:** `app/globals.css` (modify)

The `@theme inline {}` block maps CSS custom properties to Tailwind utility classes. To use custom colors in components:
```typescript
// Tailwind utility classes reference the mapped tokens
className="bg-background text-foreground"        // page background
className="bg-card text-card-foreground"          // card surfaces
className="bg-primary text-primary-foreground"    // CTA buttons
className="border-border"                         // borders
```

### `cn()` Utility for Class Merging
**Source:** `lib/utils.ts` (lines 1-6)
**Apply to:** All components using conditional Tailwind classes
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
Usage pattern: `className={cn("base-classes", condition && "conditional-classes")}`

### Path Alias Pattern
**Source:** `tsconfig.json` (lines 21-23)
**Apply to:** All imports
```json
"paths": {
  "@/*": ["./*"]
}
```
All component imports use `@/` prefix: `import { cn } from "@/lib/utils"`, `import { Button } from "@/components/ui/button"`

### Server Component Concise Type Pattern
**Source:** `app/layout.tsx` (lines 25-29)
**Apply to:** All Server Components
```typescript
export default function ComponentName({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
```

### Client Component Pattern
**Source:** RESEARCH.md (Pattern 1, lines 246-285)
**Apply to:** `NavBar`, `GalleryGrid`, `GalleryLightbox`, `VideoGrid`, `VideoThumbnail`
```typescript
'use client'

import { useState } from "react"
// ... other imports

export default function InteractiveComponent() {
  const [state, setState] = useState(initialValue)
  // ... effects, handlers
  return (/* JSX */)
}
```

## No Analog Found

Files with no close match in the codebase (planner should use RESEARCH.md patterns and UI-SPEC interaction specs instead):

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `components/NavBar.tsx` | component | event-driven | No client components exist in fresh project |
| `components/HeroSection.tsx` | component | request-response | No custom Server Components exist |
| `components/AboutSection.tsx` | component | request-response | No custom Server Components exist |
| `components/GalleryGrid.tsx` | component | CRUD (Drive API) | No API-fetching client components exist |
| `components/GalleryLightbox.tsx` | component | event-driven | No dialog/overlay components exist |
| `components/VideoGrid.tsx` | component | event-driven | No embed/thumbnail components exist |
| `components/VideoThumbnail.tsx` | component | request-response | No card/thumbnail components exist |
| `components/FormPlaceholder.tsx` | component | request-response | No custom Server Components exist |
| `components/Footer.tsx` | component | request-response | No custom Server Components exist |
| `.env.local` | config | — | No env files exist in project yet |

**For all "no analog" files:** Use RESEARCH.md code examples (Pattern 1: Server Component Islands, Pattern 2: Google Drive Fetch, YouTube/Instagram embed helpers) and UI-SPEC interaction state tables as primary references.

## Metadata

**Analog search scope:** `app/`, `lib/`, `components/`, `hooks/`
**Files scanned:** 7 (layout.tsx, page.tsx, globals.css, utils.ts, components.json, package.json, tsconfig.json)
**Pattern extraction date:** 2026-07-02
**Explicitly missing analogs due to:** Project is a fresh `create-next-app` bootstrap — this is the first phase, so no custom components exist to use as analogs.
