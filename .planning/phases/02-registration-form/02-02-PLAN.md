---
phase: 02-registration-form
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/RegistrationProvider.tsx
  - components/PaymentPlaceholder.tsx
autonomous: true
requirements:
  - FORM-04
  - FORM-07

must_haves:
  truths:
    - RegistrationProvider wraps the page and provides setRegistrationData + data to consumers
    - useRegistration() hook throws meaningful error if called outside provider
    - PaymentPlaceholder renders an invisible section with id="payment" and scroll-mt-16
  artifacts:
    - components/RegistrationProvider.tsx
    - components/PaymentPlaceholder.tsx
  key_links:
    - RegistrationProvider.tsx exports RegistrationData type → consumed by RegistrationForm (Plan 02-04) and Phase 3 payment/sheets
    - PaymentPlaceholder id="payment" → scroll target for success toast in RegistrationForm (Plan 02-04)
---

<objective>
Create the React context provider for cross-phase form data handoff, and the payment placeholder section for auto-scroll targeting.

Purpose: RegistrationProvider establishes the Phase 2→3 contract so form data persists across form submission and is readable by Phase 3 payment/sheets logic. PaymentPlaceholder gives the form's success handler a scroll target for "Now proceed to payment" navigation.

Output: components/RegistrationProvider.tsx (context provider + hook), components/PaymentPlaceholder.tsx (stub section).
</objective>

<execution_context>
@/home/ani/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ani/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-registration-form/02-CONTEXT.md
@.planning/phases/02-registration-form/02-RESEARCH.md
@.planning/phases/02-registration-form/02-PATTERNS.md
@.planning/phases/02-registration-form/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RegistrationProvider context</name>
  <files>components/RegistrationProvider.tsx</files>
  <action>
    Create `components/RegistrationProvider.tsx` per RESEARCH.md "React Context Provider Pattern" (lines 506-546) and PATTERNS.md "Full context provider pattern" (lines 121-161).

    Requirements per D-15 and D-16:
    - `"use client"` directive (first line)
    - `RegistrationData` interface with ALL fields from the form (must be stable for Phase 3 contract):
      - `name: string`
      - `contact: string`
      - `email: string`
      - `aadhar: string`
      - `bringingGuest: boolean`
      - `guestName?: string` (optional)
      - `guestAge?: string` (optional)
      - `about?: string` (optional)
      - `social?: string` (optional)
    - `RegistrationContextValue` interface: `{ data: RegistrationData | null; setRegistrationData: (data: RegistrationData) => void }`
    - `RegistrationProvider` component using `useState<RegistrationData | null>(null)` — writes to context only once on successful submit (Pitfall 5: avoid re-render cascade by keeping form state in RHF while editing, only writing to context on submit success)
    - `useRegistration()` custom hook that throws `"useRegistration must be inside RegistrationProvider"` if context is null
    - Named exports for RegistrationProvider and useRegistration
    - Default export for RegistrationProvider

    Do NOT put RegistrationData in localStorage or sessionStorage (per RESEARCH.md Known Threat Patterns — Aadhar is sensitive, keep in-memory only).

    Do NOT add any form logic here — context is a pure storage mechanism. Form logic lives in RegistrationForm.tsx (Plan 02-04).

    Import pattern per PATTERNS.md:
    ```typescript
    "use client"

    import { createContext, useContext, useState, type ReactNode } from "react"
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict components/RegistrationProvider.tsx 2>&1</automated>
  </verify>
  <done>
    - RegistrationProvider component compiles with no TypeScript errors
    - useRegistration() is exported and typed
    - RegistrationData interface covers all 9 form fields (5 required + 4 optional)
    - Context is created with null default — no data in localStorage
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PaymentPlaceholder stub section</name>
  <files>components/PaymentPlaceholder.tsx</files>
  <action>
    Create `components/PaymentPlaceholder.tsx` per UI-SPEC.md "Payment Placeholder" section (lines 153-161).

    Requirements per UI-SPEC:
    - Server component (no `"use client"` directive) — no hooks or interactivity
    - Renders `<section id="payment" className="scroll-mt-16" aria-hidden="true" />`
    - Must be a `<section>` element (not a `<div>`) with `id="payment"` — per UI-SPEC: "Cannot be a bare div with id — must be a section element with scroll-mt-16 to account for nav bar offset"
    - `scroll-mt-16` accounts for NavBar height when auto-scrolling
    - `aria-hidden="true"` makes it invisible to screen readers (it's a functional scroll target, not content)
    - No visual rendering at all
    - Named export: `export function PaymentPlaceholder()`
    - Default export: `export default function PaymentPlaceholder()`

    This component is consumed by Plan 02-04 which adds it to page.tsx below the RegistrationForm section.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict components/PaymentPlaceholder.tsx 2>&1</automated>
  </verify>
  <done>
    - PaymentPlaceholder compiles without TypeScript errors
    - File is a Server Component (no "use client")
    - Renders `<section id="payment" className="scroll-mt-16" aria-hidden="true" />`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RegistrationProvider (client) → consumer components | Shared context passes form data across component tree |
| React Context | In-memory only — no persistence layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | RegistrationProvider context stores Aadhar in memory | low | accept | Per RESEARCH.md Known Threat Patterns: React context is in-memory only, not serialized to localStorage. Aadhar is only in memory until Phase 3 writes to Google Sheets over HTTPS. Phase 3 ensures HTTPS transport. |
| T-02-03 | Spoofing | useRegistration() called outside Provider | low | mitigate | Hook throws "useRegistration must be inside RegistrationProvider" — fails fast with clear error message. TypeScript catches import issues. |
</threat_model>

<verification>
- `npx tsc --noEmit --strict` passes for both files
- RegistrationProvider exports RegistrationData type, RegistrationProvider component, and useRegistration hook
- PaymentPlaceholder renders section with id="payment"
</verification>

<success_criteria>
- RegistrationProvider compiles and exports RegistrationData type, RegistrationProvider, useRegistration
- PaymentPlaceholder compiles and provides scroll target
- Both files committed with clear messages
</success_criteria>
