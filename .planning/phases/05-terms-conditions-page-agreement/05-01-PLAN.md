---
phase: 05-terms-conditions-page-agreement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/terms.ts
  - lib/validations.ts
  - tests/validation.test.ts
autonomous: true
requirements:
  - TC-01
  - TC-04
  - TC-05

must_haves:
  truths:
    - T&C content is defined in a typed data file, not inline in the page component (D-05)
    - "Last updated: July 14, 2026" is a static string in the data file, not env-driven (D-06)
    - Registration Zod schema requires `termsAccepted` to be exactly `true` or validation fails
    - Validation tests prove `termsAccepted` must be `true` and rejects missing/false values
  artifacts:
    - lib/terms.ts
  key_links:
    - Zod schema changes flow into RegistrationForm -> form.handleSubmit blocks on failed validation
---

<objective>
Create the typed Terms & Conditions data file and extend the Zod registration schema with a required `termsAccepted` literal field. This is Wave 1 foundation — subsequent plans depend on it.

Purpose: Separate T&C content from presentation (per D-05), and ensure the checkbox agreement is enforced at the validation layer before the form can be submitted (per TC-04, TC-05).
Output: lib/terms.ts, updated lib/validations.ts, extended validation tests
</objective>

<execution_context>
@/home/ani/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ani/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase SUMMARYs only if types/exports are needed

@lib/validations.ts
@tests/validation.test.ts
@components/RegistrationProvider.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/terms.ts with typed T&C clause data</name>
  <files>lib/terms.ts</files>

  <read_first>
    @lib/validations.ts
    @lib/site-config.ts — to match export style conventions
  </read_first>

  <behavior>
    - Export `TermClause` interface with `id: string`, `title: string`, `content: string`
    - Export `lastUpdated` string constant = `"July 14, 2026"`
    - Export `termsClauses` array of 8 `TermClause` objects with these titles:
      1. Introduction
      2. Membership & Eligibility
      3. Event Participation
      4. Code of Conduct
      5. Payment & Refunds
      6. Liability Disclaimer
      7. Privacy
      8. Contact & Modifications
    - Content strings match the 05-RESEARCH.md clauses (the 05-UI-SPEC copywriting contract)
  </behavior>

  <action>
    Create `lib/terms.ts`:

    ```ts
    export interface TermClause {
      id: string
      title: string
      content: string
    }

    export const lastUpdated = "July 14, 2026"

    export const termsClauses: TermClause[] = [
      // 8 clauses from RESEARCH.md content — verbatim copy
    ]
    ```

    Clause IDs: `introduction`, `membership-eligibility`, `event-participation`, `code-of-conduct`, `payment-refunds`, `liability`, `privacy`, `contact-modification`.

    Clause content verbatim from 05-RESEARCH.md §Code Examples — the full paragraph per clause as written in the research doc lines 276-323.

    Export pattern: named exports (`export const`, `export interface`), no default export. Follows the same convention as `lib/site-config.ts` and `lib/validations.ts`.

    Use JSDoc `/** ... */` comments on the interface and each clause explaining the purpose (following codebase convention seen in `lib/site-config.ts`).
  </action>

  <acceptance_criteria>
    - `lib/terms.ts` file exists at the project root
    - Exports `TermClause` interface, `lastUpdated` string, and `termsClauses` as `TermClause[]`
    - `lastUpdated` === `"July 14, 2026"`
    - `termsClauses.length === 8`
    - Each clause has a unique `id`, non-empty `title`, non-empty `content`
    - Importing via `import { termsClauses, lastUpdated } from "@/lib/terms"` resolves without errors
    - TypeScript compilation: `npx tsc --noEmit` passes with no errors related to lib/terms.ts
  </acceptance_criteria>

  <verify>
    <human-check>Verify the file compiles: `npx tsc --noEmit lib/terms.ts --moduleResolution bundler --module esnext --target esnext 2>&1 | head -20`</human-check>
  </verify>

  <done>lib/terms.ts exists with 8 typed clauses, compiles cleanly with TypeScript, exports match expected interface</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add termsAccepted to registrationSchema and extend validation tests</name>
  <files>lib/validations.ts, tests/validation.test.ts</files>

  <read_first>
    @lib/validations.ts — current schema structure
    @tests/validation.test.ts — existing test patterns (validBase, describe blocks, safeParse assertions)
    @components/RegistrationProvider.tsx — RegistrationData type (do NOT add termsAccepted to it per ponytail[defer])
  </read_first>

  <behavior>
    - `registrationSchema` gains `termsAccepted: z.literal(true, { message: "You must agree to the Terms & Conditions" })`
    - `RegistrationFormData` type auto-derives `termsAccepted: true` (via z.infer)
    - Validation tests add a `describe("termsAccepted")` block with these cases:
      - accepts `{ ...validBase, termsAccepted: true }`
      - rejects `{ ...validBase, termsAccepted: false }`
      - rejects `{ ...validBase, termsAccepted: undefined }` (missing field)
      - rejects `{ ...validBase, termsAccepted: "yes" }` (non-boolean)
    - `RegistrationProvider` RegistrationData type does NOT include termsAccepted — Zod validation proves acceptance before onSubmit, adding it is speculative (per research §Open Questions Q3 recommendation)
  </behavior>

  <action>
    In `lib/validations.ts`:

    Add `termsAccepted` to the `registrationSchema` object definition, after the existing `social` field and before the closing `})`:

    ```ts
    termsAccepted: z.literal(true, {
      message: "You must agree to the Terms & Conditions",
    }),
    ```

    Do NOT modify `RegistrationData` in `RegistrationProvider.tsx`. The `onSubmit` handler in RegistrationForm already destructures only the fields it passes to `setRegistrationData` — `termsAccepted` will be present in the validated `data` object but silently omitted from the provider call. This is correct: the Zod schema proves acceptance before `handleSubmit` fires; the downstream payment pipeline does not need T&C status.

    In `tests/validation.test.ts`:

    Add a new `describe("termsAccepted")` block after the existing `describe("social")` block (after line 354). Add these tests using the existing `validBase` fixture:

    1. `it("accepts termsAccepted: true")` — expects `result.success === true`
    2. `it("rejects termsAccepted: false")` — expects `result.success === false`, error path contains `termsAccepted`
    3. `it("rejects missing termsAccepted field")` — spread omit `termsAccepted` from validBase, expects `result.success === false`, error path contains `termsAccepted`
    4. `it("rejects termsAccepted with non-boolean value")` — sets `termsAccepted: "yes"`, expects `result.success === false`

    Follow the existing assertion patterns:
    - Use `validBase` + spread with override
    - For rejects: check `result.success === false`, then `expect(result.error?.issues[0]?.path).toContain("termsAccepted")` or equivalent
    - TypeScript note: `termsAccepted` is not in `validBase` so it will be spread as an extra property — add `as const` or `// @ts-expect-error - termsAccepted is the field under test` as needed
  </action>

  <acceptance_criteria>
    - `registrationSchema` includes `termsAccepted: z.literal(true, ...)`
    - `safeParse({ ...validBase, termsAccepted: true })` returns `success: true`
    - `safeParse({ ...validBase, termsAccepted: false })` returns `success: false`
    - `safeParse({ ...validBase })` (missing field) returns `success: false`
    - `safeParse({ ...validBase, termsAccepted: "yes" })` returns `success: false`
    - Error message contains "You must agree to the Terms & Conditions"
    - `npx vitest run tests/validation.test.ts` — all existing + new tests pass
    - `RegistrationData` type is unchanged (no `termsAccepted` field added)
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run tests/validation.test.ts --reporter=verbose</automated>
  </verify>

  <done>termsAccepted field added to schema with z.literal(true) validation, 4 new tests pass in validation.test.ts, RegistrationData unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser | Zod schema runs in the browser; validation is client-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | Zod `z.literal(true)` in browser | low | accept | Client-side validation bypass has no impact — T&C acceptance only gates the client UI flow. The existing payment/sheets/email pipeline does not depend on T&C status. A future server-side Zod check would re-validate the same schema. |
| T-05-SC | Tampering | npm packages | low | mitigate | No new packages installed this phase. All deps (zod, radix-ui, next) already in package.json. |
</threat_model>

<verification>
- `npx vitest run tests/validation.test.ts --reporter=verbose` — all tests pass (existing + new)
- `npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- lib/terms.ts created with 8 clauses, compiles cleanly
- registrationSchema validates termsAccepted: true, rejects anything else
- All validation tests pass
</success_criteria>

<output>
Create `.planning/phases/05-terms-conditions-page-agreement/05-01-SUMMARY.md` when done
</output>
